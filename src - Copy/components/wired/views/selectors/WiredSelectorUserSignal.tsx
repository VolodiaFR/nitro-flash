import { FC } from 'react';
import { WiredSelectorBaseView } from './WiredSelectorBaseView';

export const WiredSelectorUserSignal: FC<{}> = () =>
{

    return (
        <WiredSelectorBaseView hasSpecialInput={true} save={null} requiresFurni={0}>
            
        </WiredSelectorBaseView>
    );
};
